import styled from 'styled-components'

export const Main = styled.main`
    position: absolute;
    z-index: 0;
    margin: 0 1rem;
`

export const CardPopup = styled.div`
    position: absolute;
    z-index: 1;
`